---
import Image from "astro/components/Image.astro";
import heroBanner from "../images/hero3.jpg";
---

<section class="hero" id="hero">
	<div class="hero-content">
		<div class="section-header">
			<h1>Retrouvez clarté et confiance grâce à la PNL</h1>
			<p class="section-subtitle">
				Découvrez comment le coaching PNL peut vous aider à dépasser vos blocages et
				atteindre vos objectifs personnels et professionnels.
			</p>
		</div>
		<a href="#contact" class="cta-btn">
			Prenez votre premier rendez-vous gratuit
		</a>
	</div>
	<Image src={heroBanner} alt="Illustration Hero" class="hero-img" />
</section>

<style lang="scss">
	.hero {
		position: relative;
		height: 80vh;
		display: flex;
		align-items: center;
		overflow: hidden;

		.hero-content {
			position: relative;
			z-index: 2;
			width: 100%;
			max-width: 1200px;
			margin: 0 auto;
			padding: 0 20px;
			text-align: center;
		}

		.section-header {
			margin-bottom: 40px;

			h1 {
				color: $text-color;
				font-size: 3.5em;
				margin-bottom: 20px;
				position: relative;
				display: inline-block;

				&:after {
					content: "";
					position: absolute;
					bottom: -15px;
					left: 50%;
					transform: translateX(-50%);
					width: 80px;
					height: 4px;
					background-color: $primary-color;
					border-radius: 2px;
				}
			}

			.section-subtitle {
				font-size: 1.4em;
				max-width: 800px;
				margin: 30px auto;
				color: $text-color;
				line-height: 1.6;
			}
		}

		.cta-btn {
			display: inline-block;
			background-color: $secondary-color;
			color: $text-white;
			padding: 20px 40px;
			text-decoration: none;
			border-radius: 8px;
			font-weight: bold;
			font-size: 1.2em;
			transition:
				transform 0.3s ease,
				background-color 0.3s ease,
				box-shadow 0.3s ease;
			box-shadow: $shadow-md;

			&:hover {
				transform: translateY(-2px);
				background-color: $hover-color-alt;
				box-shadow: $shadow-lg;
			}
		}

		.hero-img {
			position: absolute;
			top: 0;
			left: 0;
			width: 100%;
			height: 100%;
			object-fit: cover;
			z-index: 1;
			opacity: 0.8;
		}
	}

	@media (max-width: $mobile) {
		.hero {
			height: 100vh;

			.section-header {
				h1 {
					font-size: 2.5em;
				}

				.section-subtitle {
					font-size: 1.2em;
					padding: 0 20px;
				}
			}

			.cta-btn {
				padding: 15px 30px;
				font-size: 1.1em;
				width: 90%;
				max-width: 350px;
			}
		}
	}
</style>

---
import { Image } from "astro:assets";
import methodImage from "../images/method.avif";
---

<section class="section" id="methodology">
	<div class="section-header">
		<h2>Comment se déroule une séance ?</h2>
		<p class="section-subtitle">
			Une approche structurée et personnalisée pour atteindre vos objectifs
		</p>
	</div>

	<div class="method-container">
		<div class="method-content">
			<p class="method-intro">
				Chaque séance est un moment d'échange où nous travaillons ensemble sur vos
				objectifs. Voici les grandes étapes :
			</p>
			<div class="method-steps">
				<div class="step-card">
					<div class="step-number">1</div>
					<div class="step-content">
						<h3>Phase d'écoute</h3>
						<p>Identification de vos besoins et objectifs</p>
					</div>
				</div>

				<div class="step-card">
					<div class="step-number">2</div>
					<div class="step-content">
						<h3>Techniques PNL</h3>
						<p>Exercices pratiques et mise en situation</p>
					</div>
				</div>

				<div class="step-card">
					<div class="step-number">3</div>
					<div class="step-content">
						<h3>Plan d'action</h3>
						<p>Mise en place d'outils concrets pour avancer</p>
					</div>
				</div>
			</div>
		</div>
		<div class="method-image">
			<Image src={methodImage} alt="Illustration de la méthodologie" />
		</div>
	</div>
</section>

<style lang="scss">
	.section {
		display: flex;
		flex-direction: column;
		padding: 60px 20px;
		background-color: $bg-color;

		.section-header {
			text-align: center;
			margin-bottom: 50px;

			h2 {
				color: $text-color;
				font-size: 2.5em;
				margin-bottom: 15px;
				position: relative;
				display: inline-block;

				&:after {
					content: "";
					position: absolute;
					bottom: -10px;
					left: 50%;
					transform: translateX(-50%);
					width: 60px;
					height: 3px;
					background-color: $primary-color;
					border-radius: 2px;
				}
			}

			.section-subtitle {
				color: $text-light;
				font-size: 1.1em;
				max-width: 600px;
				margin: 0 auto;
			}
		}
	}

	.method-container {
		display: flex;
		justify-content: space-between;
		align-items: center;
		gap: 40px;
		max-width: 1200px;
		margin: 0 auto;
		padding: 0 20px;
	}

	.method-content {
		flex: 1;

		.method-intro {
			font-size: 1.1em;
			line-height: 1.6;
			color: $text-color;
			margin-bottom: 30px;
		}
	}

	.method-steps {
		display: flex;
		flex-direction: column;
		gap: 20px;
	}

	.step-card {
		display: flex;
		align-items: center;
		gap: 20px;
		background-color: $bg-light;
		padding: 20px;
		border-radius: $border-radius-lg;
		box-shadow: $shadow-md;
		transition: transform 0.3s ease;

		&:hover {
			transform: translateX(10px);
		}

		.step-number {
			background-color: $primary-color;
			color: $text-white;
			width: clamp(30px, 5vw, 40px);
			height: clamp(30px, 5vw, 40px);
			border-radius: 50%;
			display: flex;
			align-items: center;
			justify-content: center;
			font-weight: bold;
			font-size: clamp(0.8em, 1vw + 0.8em, 1.2em);
			aspect-ratio: 1;
			flex-shrink: 0;
		}

		.step-content {
			h3 {
				color: $text-color;
				margin-bottom: 5px;
				font-size: clamp(0.8em, 1vw + 0.8em, 1.2em);
			}

			p {
				color: $text-light;
				font-size: clamp(0.7em, 1vw + 0.7em, 1em);
				margin: 0;
			}
		}
	}

	.method-image {
		flex: 1;
		max-width: 500px;
		height: 100%;

		img {
			width: 100%;
			height: 437px;
			border-radius: 15px;
			box-shadow: 0 4px 6px rgba(0, 0, 0, 0.1);
			transition: transform 0.3s ease;
			object-fit: cover;

			&:hover {
				transform: scale(1.02);
			}
		}
	}

	@media (max-width: $mobile) {
		.method-container {
			flex-direction: column-reverse;
		}

		.method-image {
			max-width: 100%;
		}

		.step-card {
			&:hover {
				transform: translateX(0);
				transform: translateY(-5px);
			}
		}

		.section-header {
			h2 {
				font-size: 2em;
			}
		}
	}
</style>

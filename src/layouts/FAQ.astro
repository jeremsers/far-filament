<section class="section" id="faq">
	<div class="section-header">
		<h2>Questions fréquentes</h2>
		<p class="section-subtitle">
			Retrouvez les réponses aux questions les plus courantes sur le coaching PNL
		</p>
	</div>

	<div class="faq-container">
		<div class="faq-list">
			<div class="faq-item">
				<details>
					<summary>Comment fonctionne la PNL ?</summary>
					<div class="faq-answer">
						<p>
							La PNL est une approche qui utilise le langage et la psychologie
							cognitive pour améliorer la communication et le développement personnel.
							Elle permet de :
						</p>
						<ul>
							<li>Comprendre vos schémas de pensée</li>
							<li>Modifier des comportements limitants</li>
							<li>Développer de nouvelles ressources</li>
							<li>Atteindre vos objectifs plus efficacement</li>
						</ul>
					</div>
				</details>
			</div>

			<div class="faq-item">
				<details>
					<summary>Combien de séances sont recommandées ?</summary>
					<div class="faq-answer">
						<p>
							Tout dépend de votre situation et de vos objectifs. Une première séance
							permet d'évaluer vos besoins et de définir un parcours adapté.
							En général, un accompagnement peut varier de 3 à 10 séances.
						</p>
					</div>
				</details>
			</div>

			<div class="faq-item">
				<details>
					<summary>Les séances peuvent-elles se faire à distance ?</summary>
					<div class="faq-answer">
						<p>
							Oui, les séances peuvent se faire en visioconférence avec la même
							efficacité qu'en présentiel. J'utilise des outils adaptés pour garantir
							une qualité d'accompagnement optimale.
						</p>
					</div>
				</details>
			</div>

			<div class="faq-item">
				<details>
					<summary>Comment se déroule la première séance ?</summary>
					<div class="faq-answer">
						<p>La première séance est un moment d'échange qui permet de :</p>
						<ul>
							<li>Faire connaissance et créer un lien de confiance</li>
							<li>Comprendre vos attentes et vos objectifs</li>
							<li>Expliquer ma méthode de travail</li>
							<li>Définir ensemble un plan d'accompagnement adapté</li>
						</ul>
					</div>
				</details>
			</div>
		</div>
	</div>
</section>

<style lang="scss">
	.section {
		display: flex;
		flex-direction: column;
		padding: clamp(30px, 5vw, 60px) clamp(15px, 3vw, 20px);
		background: linear-gradient(to bottom, $bg-color, $bg-light);

		.section-header {
			text-align: center;
			margin-bottom: clamp(30px, 5vw, 50px);

			h2 {
				color: #2c3e50;
				font-size: clamp(2em, 5vw, 2.5em);
				margin-bottom: 15px;
				position: relative;
				display: inline-block;

				&:after {
					content: "";
					position: absolute;
					bottom: -10px;
					left: 50%;
					transform: translateX(-50%);
					width: clamp(40px, 8vw, 60px);
					height: 3px;
					background-color: #6b8e23;
					border-radius: 2px;
				}
			}

			.section-subtitle {
				color: #666;
				font-size: clamp(1em, 2.5vw, 1.1em);
				max-width: 600px;
				margin: 0 auto;
				padding: 0 clamp(10px, 3vw, 20px);
			}
		}
	}

	.faq-container {
		max-width: 800px;
		margin: 0 auto;
		padding: 0 clamp(10px, 3vw, 20px);
		width: 100%;
	}

	.faq-list {
		display: flex;
		flex-direction: column;
		gap: clamp(15px, 3vw, 20px);
	}

	.faq-item {
		width: 100%;
		max-width: 700px;
		margin: 0 auto;

		details {
			background-color: $bg-color;
			border-radius: $border-radius-lg;
			box-shadow: $shadow-md;
			transition: all 0.3s ease;
			width: 100%;

			&[open] {
				background-color: white;
				box-shadow: $shadow-lg;

				summary {
					border-bottom: 1px solid $border-color;
					border-radius: 15px 15px 0 0;

					&::before {
						transform: rotate(90deg);
					}
				}
			}

			summary {
				padding: clamp(15px, 3vw, 20px) clamp(20px, 4vw, 25px);
				cursor: pointer;
				position: relative;
				font-weight: 600;
				list-style: none;
				color: $text-color;
				transition: all 0.3s ease;
				font-size: clamp(0.95em, 2.5vw, 1em);

				&::-webkit-details-marker {
					display: none;
				}

				&::before {
					content: "›";
					position: absolute;
					right: clamp(15px, 3vw, 25px);
					font-size: 1.5em;
					transition: transform 0.3s ease;
					color: $primary-color;
				}

				&:hover {
					background-color: rgba(107, 142, 35, 0.1);
					border-radius: $border-radius-lg;
				}
			}
		}

		.faq-answer {
			padding: clamp(15px, 3vw, 20px) clamp(20px, 4vw, 25px);
			color: $text-light;
			line-height: 1.6;
			font-size: clamp(0.9em, 2.5vw, 1em);

			p {
				margin: 0 0 15px 0;
			}

			ul {
				margin: 0;
				padding-left: clamp(15px, 3vw, 20px);

				li {
					margin: clamp(6px, 2vw, 8px) 0;
					position: relative;
					padding-left: 5px;

					&::before {
						content: "•";
						color: $primary-color;
						position: absolute;
						left: -15px;
					}
				}
			}
		}
	}

	@media (max-width: $mobile) {
		.faq-container {
			padding: 0 10px;
		}

		.faq-item {
			details {
				summary {
					padding-right: 40px;
				}
			}
		}
	}
</style>

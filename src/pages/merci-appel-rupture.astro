---
import Layoutalt from "../layouts/baselayout/Layoutalt.astro";
---

<Layoutalt title="Merci pour votre réservation - Essence et Conscience">
	<section class="section">
		<div class="container">
			<div class="content">
				<h1>Merci pour votre réservation !</h1>
				<p>
					Votre rendez-vous a bien été pris en compte. Un email de confirmation vous
					a été envoyé avec tous les détails.
				</p>
				<p>
					Si vous avez des questions avant notre rencontre, n'hésitez pas à me
					contacter.
				</p>
				<a href="/" class="cta-btn">Retour à l'accueil</a>
			</div>
		</div>
	</section>
</Layoutalt>

<style lang="scss">
	.content {
        background-color: $bg-light;
        border-radius: 10px;
		text-align: center;
		padding: 4rem 0;
		display: flex;
		flex-direction: column;
		align-items: center;
		gap: 2rem;

		h1 {
			font-size: clamp(2rem, 5vw, 3rem);
			color: var(--text-color);
			margin-bottom: 1rem;
		}

		p {
			font-size: clamp(1rem, 2vw, 1.2rem);
			max-width: 600px;
			margin: 0 auto;
			color: #4a5568;
		}

		.cta-btn {
			display: inline-block;
			background-color: $secondary-color;
			color: $text-white;
			padding: clamp(8px, 1.5vw, 12px) clamp(15px, 2vw, 25px);
			border-radius: 30px;
			font-weight: 600;
			font-size: clamp(0.85rem, 1.5vw, 1rem);
			transition: all 0.3s ease;
			white-space: nowrap;
			border: 2px solid transparent;

			&:hover {
				transform: translateY(-3px);
				box-shadow: 0 7px 14px rgba(0, 0, 0, 0.2);
				background-color: darken($secondary-color, 5%);
			}

			&:active {
				transform: translateY(-1px);
				box-shadow: 0 3px 8px rgba(0, 0, 0, 0.2);
			}
		}
	}
</style>
